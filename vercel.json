{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": "nestjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter backend prisma:generate && pnpm --filter backend build",
  "outputDirectory": "apps/backend/dist"
}
